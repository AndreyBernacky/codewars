const a = "dev";
const b = "Lab";

const name = a + b;

//

function addFive(num) {
    var total = num + 5
    return total
  }

  //

  function move (position, roll) {
    return position + roll*2;
  }

  //
  function opposite(number) {
  return number*(-1);
}
